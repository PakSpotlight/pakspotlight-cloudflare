# Pak Spotlight — Fast Comedy Fix

This build keeps the existing Supabase data/Admin setup and fixes:
- faster first render
- external compressed logo at `/logo.png`
- pending thumbnail hydration no longer sends the user back to Home
- Comedy/Serial episode pages keep one embedded YouTube player
- episode controls switch the same player
- responsive episode controls

Deploy with the existing Cloudflare Pages/Workers GitHub setup.
