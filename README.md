Pak Spotlight — Cloudflare Worker static website.

Uses the existing Supabase Drama table. Series are grouped by series_name and episode_number. Long Play includes telefilms/long plays; there is no separate TeleFilm tab. Editing without choosing a new image preserves the existing thumbnail.updated
